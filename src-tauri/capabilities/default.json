{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the local Comote daemon UI (served from 127.0.0.1:16208) and the bundled boot page to call the desktop commands they need.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:16208"]
  },
  "permissions": [
    "core:default",
    "allow-open-external",
    "allow-get-keep-daemon-alive",
    "allow-set-keep-daemon-alive"
  ]
}
